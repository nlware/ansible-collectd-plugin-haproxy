# test file
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - name: include tasks
      include: "{{ playbook_dir }}/tasks/pre.yml"
    - name: include vars
      include_vars: "{{ playbook_dir }}/vars/main.yml"
  roles:
    - ../../
